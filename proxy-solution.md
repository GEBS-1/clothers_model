# 📋 Пошаговая инструкция: Настройка прокси через Cloudflare Workers

## 🎯 Цель
Обойти блокировку встраивания Hugging Face Space через iframe, чтобы демо работало прямо на твоей странице.

---

## 📝 Шаг 1: Регистрация на Cloudflare

1. Открой https://dash.cloudflare.com/sign-up
2. Зарегистрируйся (можно через Google/GitHub)
3. **Важно:** Выбери бесплатный план (Free) - его достаточно

---

## 📝 Шаг 2: Создание Worker

1. После входа в панель Cloudflare, найди в левом меню **"Workers & Pages"**
2. Нажми **"Create application"** или **"Create Worker"**
3. Выбери **"Create Worker"** (не Pages!)

---

## 📝 Шаг 3: Настройка Worker

1. **Имя Worker:**
   - Введи любое имя, например: `huggingface-proxy` или `vton-proxy`
   - Это будет часть твоего URL: `huggingface-proxy.твой-поддомен.workers.dev`

2. **Код Worker:**
   - Удали весь код, который там есть по умолчанию
   - Открой файл `cloudflare-worker.js` из этого проекта
   - Скопируй **весь код** из этого файла
   - Вставь его в редактор Cloudflare

3. **Сохрани и разверни:**
   - Нажми кнопку **"Save and deploy"** (внизу справа)
   - Дождись сообщения "Successfully deployed"

---

## 📝 Шаг 4: Получение URL прокси

1. После успешного деплоя, Cloudflare покажет тебе URL
2. Он будет выглядеть так: `https://huggingface-proxy.твой-поддомен.workers.dev`
3. **Скопируй этот URL** - он понадобится в следующем шаге

---

## 📝 Шаг 5: Обновление index.html

1. Открой файл `index.html` в проекте
2. Найди строку с iframe (примерно строка 59):
   ```html
   <iframe 
     id="vton-iframe"
     src="https://kwai-kolors-kolors-virtual-try-on.hf.space/" 
   ```
3. Замени URL на твой прокси URL:
   ```html
   <iframe 
     id="vton-iframe"
     src="https://huggingface-proxy.твой-поддомен.workers.dev/" 
   ```
4. Сохрани файл

---

## 📝 Шаг 6: Проверка

1. Загрузи обновленный `index.html` на GitHub Pages
2. Открой сайт в браузере
3. Перейди к секции "Как это работает"
4. **Если все правильно:** iframe должен загрузиться и показать Gradio интерфейс
5. **Если не работает:** открой консоль браузера (F12) и проверь ошибки

---

## 🔧 Возможные проблемы

### Проблема: "Worker failed to deploy"
**Решение:** 
- Проверь, что скопировал весь код из `cloudflare-worker.js`
- Убедись, что нет синтаксических ошибок

### Проблема: "403 Forbidden" в iframe
**Решение:**
- Убедись, что URL в `index.html` заканчивается на `/` (слеш)
- Проверь, что Worker успешно развернут (статус "Active")

### Проблема: "CORS error"
**Решение:**
- Код Worker уже содержит настройки CORS
- Если ошибка остается, проверь, что Worker правильно проксирует запросы

---

## ✅ Готово!

После выполнения всех шагов, твой iframe должен работать и показывать Gradio интерфейс прямо на странице.

**Время настройки:** ~5-10 минут

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверь консоль браузера (F12 → Console)
2. Проверь Network tab (F12 → Network) - какие запросы идут
3. Убедись, что Worker развернут и активен в панели Cloudflare
