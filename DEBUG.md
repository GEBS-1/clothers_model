# 🔍 Диагностика проблем с iframe

## Проблема: iframe не загружается (белый экран)

### Шаг 1: Проверь Worker напрямую

Открой в браузере:
```
https://clothersmodel.gebraunt.workers.dev/
```

**Ожидаемый результат:** Должна открыться страница Hugging Face Space (Gradio интерфейс)

**Если ошибка:**
- Worker не работает или не развернут
- Проверь статус в Cloudflare Dashboard → Workers & Pages

---

### Шаг 2: Проверь консоль браузера

1. Открой сайт: `https://gebs-1.github.io/clothers_model/`
2. Нажми **F12** (открыть DevTools)
3. Перейди на вкладку **Console**
4. Посмотри на ошибки (красные сообщения)

**Что искать:**
- `Failed to load resource` - Worker не отвечает
- `CORS error` - проблема с заголовками
- `403 Forbidden` - Worker заблокирован
- `X-Frame-Options` - блокировка встраивания

---

### Шаг 3: Проверь Network tab

1. В DevTools перейди на вкладку **Network**
2. Обнови страницу (F5)
3. Найди запрос к `clothersmodel.gebraunt.workers.dev`
4. Кликни на него и посмотри:
   - **Status:** должен быть `200 OK`
   - **Response Headers:** должны быть заголовки без `X-Frame-Options: DENY`
   - **Response:** должен быть HTML код

**Если Status не 200:**
- Worker не работает
- Проверь логи в Cloudflare Dashboard

---

### Шаг 4: Проверь Worker код

1. Открой Cloudflare Dashboard → Workers & Pages
2. Открой Worker `clothersmodel`
3. Проверь, что код из `cloudflare-worker.js` там есть
4. Проверь логи (Logs tab) - есть ли ошибки

---

### Шаг 5: Тест Worker вручную

Открой в новой вкладке:
```
https://clothersmodel.gebraunt.workers.dev/
```

**Если видишь Gradio интерфейс:**
- Worker работает ✅
- Проблема в iframe или настройках страницы

**Если ошибка или пусто:**
- Worker не работает ❌
- Проверь код Worker в Cloudflare Dashboard

---

## Возможные решения

### Решение 1: Worker не развернут
- Зайди в Cloudflare Dashboard
- Убедись, что Worker активен
- Если нет - нажми "Deploy" снова

### Решение 2: Неправильный URL
- Проверь, что URL в `index.html` правильный
- Должен быть: `https://clothersmodel.gebraunt.workers.dev/`
- Обрати внимание на `/` в конце

### Решение 3: Worker код неправильный
- Открой Worker в Cloudflare Dashboard
- Убедись, что код из `cloudflare-worker.js` там есть
- Проверь, что используется ES Modules формат

### Решение 4: Кэш браузера
- Очисти кэш (Ctrl+Shift+Delete)
- Или открой в режиме инкогнито (Ctrl+Shift+N)

---

## Быстрая проверка

Выполни в консоли браузера (F12 → Console):
```javascript
// Проверка iframe
const iframe = document.getElementById('vton-iframe');
console.log('Iframe URL:', iframe?.src);
console.log('Iframe loaded:', iframe?.contentWindow ? 'да' : 'нет');

// Проверка Worker напрямую
fetch('https://clothersmodel.gebraunt.workers.dev/')
  .then(r => r.text())
  .then(html => console.log('Worker response:', html.substring(0, 200)))
  .catch(e => console.error('Worker error:', e));
```

